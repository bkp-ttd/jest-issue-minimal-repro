{
    "$schema": "./../../node_modules/nx/schemas/project-schema.json",
    "targets": {
        "test": {
            "executor": "@nx/jest:jest",
            "options": {
                "jestConfig": "{projectRoot}/jest.config.json"
            }
        }
    }
}